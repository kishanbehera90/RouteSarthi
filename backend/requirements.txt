# --- Core API (Step 0: scaffold) ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27            # outbound calls + TestClient

# --- Data layer (Step 1: engine + ETL use these at import time) ---
psycopg[binary]>=3.2
psycopg-pool>=3.2

# --- Auth (signup/login, password hashing + JWT sessions) ---
bcrypt>=4.1
PyJWT>=2.9

# --- Deferred until needed ---
# SQLAlchemy>=2.0
# GeoAlchemy2>=0.15
# alembic>=1.13

# --- Cache (added in Step 4) ---
# redis>=5.1

# --- Ingestion + ML ---
# scikit-learn: delay-prediction model (load + predict at runtime; training in
#   etl/train_delay_model.py). Histogram gradient boosting — same algorithm
#   family as LightGBM but no native OpenMP wheel to deploy. Pulls numpy/scipy/joblib.
#   PINNED to the minor version the shipped delay_model.joblib was trained on:
#   sklearn does NOT guarantee unpickle compatibility across minor versions, so a
#   float to 1.10+ could silently break model load and drop the "predicted" tier
#   (it degrades to the measured/modelled fallback, so no crash — just a quiet
#   loss of the ML feature). Bump this in lockstep when you retrain the model.
scikit-learn>=1.9,<1.10
# joblib: used directly by app/delay_model.py to load the artifact. It's pulled
#   transitively by scikit-learn, but pin it explicitly since we import it by name.
joblib>=1.3
# pandas: training-time only (etl/train_delay_model.py assembles the sample frame).
pandas>=2.2
# geopandas>=1.0
# lightgbm>=4.5

# --- Tests ---
pytest>=8.0
